import React from 'react';
import stl from './loaderStyle.module.css'

const Loader = () => {
    return (
        <div className={stl.loader}>
            
        </div>
    );
};

export default Loader;